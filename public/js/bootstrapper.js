/**
 * Init data
 */
//app.dataContext.getUids(function (data) {
//    if (data.length > 0) {
//        app.pubSub.notifySubscribers(data, app.utils.subscriberType.uids);

//        // Set UID
//        app.pubSub.notifySubscribers(data[0].name, app.utils.subscriberType.uid);
//    }
//});

app.dataContext.getUids();